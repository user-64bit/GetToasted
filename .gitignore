# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem
.claude

# TypeScript incremental build cache — regenerated on every `tsc` run.
*.tsbuildinfo

# Detector regression fixtures — large (~500MB) full-block JSON snapshots
# authored on demand by the legacy harness commands (bootstrap-a-guard,
# bootstrap-arbitrage-negatives, analyze-wallet --write-fixture).
# Production code does NOT load these. Re-mine via the harness CLI when
# you need them locally; production validation uses the Jito-mining flow
# in research/mined-victims/ instead. See SUBMISSION.md and DETECTOR.md.
tests/detector/fixtures/
